# Production Environment Configuration Template

# Server Settings
PORT=5000
NODE_ENV=production

# Database
# Example: mongodb+srv://username:password@cluster.mongodb.net/church_db
MONGODB_URI=your_mongodb_connection_string_here

# Security
# Generate a strong secret (e.g., using 'openssl rand -base64 32')
JWT_SECRET=your_jwt_secret_here

# Frontend/Client Settings
# The URL where your frontend is hosted (for CORS)
CLIENT_URL=https://your-domain.com

# API Settings (for Frontend build)
# VITE_API_URL=https://your-domain.com/api
